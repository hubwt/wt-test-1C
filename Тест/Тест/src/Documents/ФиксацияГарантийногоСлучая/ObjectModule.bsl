
Процедура ОбработкаЗаполнения(ДанныеЗаполнения, ТекстЗаполнения, СтандартнаяОбработка)
	
	//
	/////+ГомзМА 07.07.2023
	//Если ТипЗнч(ДанныеЗаполнения) = Тип("ДокументСсылка.ЗаказНаряд") Тогда
	//	
	//	Клиент = ДанныеЗаполнения.Клиент;
	//	
	//	Для каждого ТекСтрокаТовары Из ДанныеЗаполнения.Товары Цикл
	//		НоваяСтрока = ТаблицаТовары.Добавить();
	//		НоваяСтрока.Номенклатура 	= ТекСтрокаТовары.Номенклатура;
	//		НоваяСтрока.Количество 		= ТекСтрокаТовары.Количество;
	//		НоваяСтрока.Цена 			= ТекСтрокаТовары.Цена;
	//		НоваяСтрока.Автомобиль 		= ТекСтрокаТовары.Автомобиль;
	//		НоваяСтрока.СкидкаСумма 	= ТекСтрокаТовары.СкидкаСумма;
	//		НоваяСтрока.СкидкаПроцент 	= ТекСтрокаТовары.СкидкаПроцент;
	//		НоваяСтрока.Сумма 			= ТекСтрокаТовары.Сумма;
	//		НоваяСтрока.Партия 			= ТекСтрокаТовары.Партия;
	//		НоваяСтрока.СуммаВсего 		= ТекСтрокаТовары.СуммаВсего;
	//		НоваяСтрока.Склад 			= ТекСтрокаТовары.Склад;
	//		НоваяСтрока.Ответственный 	= ТекСтрокаТовары.Ответственный;
	//		НоваяСтрока.СуммаНалог 		= ТекСтрокаТовары.СуммаНалог;
	//		НоваяСтрока.СуммаЗп 		= ТекСтрокаТовары.СуммаЗп;
	//		НоваяСтрока.Состояние 		= ТекСтрокаТовары.Состояние;
	//		НоваяСтрока.СуммаСНДС 		= ТекСтрокаТовары.СуммаСНДС;
	//	КонецЦикла;
	//	
	//	Для каждого ТекСтрокаРаботы Из ДанныеЗаполнения.Работы Цикл
	//		НоваяСтрока = ТаблицаРаботы.Добавить();
	//		НоваяСтрока.Работа 				= ТекСтрокаРаботы.Работа;
	//		НоваяСтрока.Количество 			= ТекСтрокаРаботы.Количество;
	//		НоваяСтрока.Нормочас 			= ТекСтрокаРаботы.Нормочас;
	//		НоваяСтрока.Цена 				= ТекСтрокаРаботы.Цена;
	//		НоваяСтрока.Сумма 				= ТекСтрокаРаботы.Сумма;
	//		НоваяСтрока.СкидкаПроцент 		= ТекСтрокаРаботы.СкидкаПроцент;
	//		НоваяСтрока.СкидкаСумма 		= ТекСтрокаРаботы.СкидкаСумма;
	//		НоваяСтрока.СуммаВсего 			= ТекСтрокаРаботы.СуммаВсего;
	//		НоваяСтрока.Содержание 			= ТекСтрокаРаботы.Содержание;
	//		НоваяСтрока.ИдентификаторСтроки = ТекСтрокаРаботы.ИдентификаторСтроки;
	//		НоваяСтрока.ВремяФакт 			= ТекСтрокаРаботы.ВремяФакт;
	//		НоваяСтрока.ВремяПлан 			= ТекСтрокаРаботы.ВремяПлан;
	//		НоваяСтрока.СуммаНалог 			= ТекСтрокаРаботы.СуммаНалог;
	//		НоваяСтрока.СуммаЗп 			= ТекСтрокаРаботы.СуммаЗп;
	//	КонецЦикла;
	//КонецЕсли;
	/////-ГомзМА 07.07.2023
	
КонецПроцедуры

Процедура ОбработкаПроведения(Отказ, Режим)


	// регистр РегистрНакопления1 Расход
	Движения.РегистрНакопления1.Записывать = Истина;
	Для Каждого ТекСтрокаТовары Из Товары Цикл
		Движение = Движения.РегистрНакопления1.Добавить();
		Движение.ВидДвижения = ВидДвиженияНакопления.Расход;
		Движение.Период = Дата;
		Движение.Склад = ТекСтрокаТовары.Склад;
		Движение.Товар = ТекСтрокаТовары.Номенклатура;
		Движение.машина = ТекСтрокаТовары.Автомобиль;
		Движение.индкод = ТекСтрокаТовары.Партия;
		Движение.Колво = ТекСтрокаТовары.Количество;
		Движение.Цена = ТекСтрокаТовары.Цена;
	КонецЦикла;
КонецПроцедуры
