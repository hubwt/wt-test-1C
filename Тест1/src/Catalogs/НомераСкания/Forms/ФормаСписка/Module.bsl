
&НаКлиенте
Процедура Команда1(Команда)
	// Вставить содержимое обработчика.
	Ф()
КонецПроцедуры

&НаСервере
Процедура Ф()
	з = Новый Запрос;
	з.Текст =    "ВЫБРАТЬ
	          |	НомераСкания.Ссылка
	          |ИЗ
	          |	Справочник.НомераСкания КАК НомераСкания"     ;
	р = з.Выполнить().Выгрузить();
	Для Каждого с из р Цикл
		с.Ссылка.ПолучитьОбъект().Удалить();
	КонецЦикла;
КонецПроцедуры
